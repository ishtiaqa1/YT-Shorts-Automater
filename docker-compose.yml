services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shorts_studio
    ports:
      - "5432:5432"
    volumes:
      - shorts_pg:/var/lib/postgresql/data

volumes:
  shorts_pg:
